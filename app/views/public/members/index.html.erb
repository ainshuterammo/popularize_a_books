<div class='container'>
  <div class='row'>
    <div class='col-md-12'>
      <h2>メンバーリスト</h2>
      <% if member_signed_in? %>
        <%= render 'public/searches/form' %>
      <% end %>
      <%= render "public/members/index", members: @members %>
      <%= paginate @members %>
    </div>
  </div>
</div>