<%= form_with(model:[book, post_comment], local: false) do |f| %>
  <%= f.text_area :comment, rows:'3',placeholder: "コメントをここに", class: "w-100 comment-textarea" %>
  <%= f.submit "コメントする", class: "btn btn-info mx-2 my-2" %>
<% end %>