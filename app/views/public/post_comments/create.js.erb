<% if @comment.errors.any? %>
  $('.error').text('コメント投稿に失敗しました。内容を確認してください。')
<% else %>
  $('.error').text('')
<% end %>
$('.comment-textarea').val("")
$('.book-comments-area').html("<%= j(render 'public/post_comments/index', post_comments: @post_comments) %>")
$('#comment-paginate').html("<%= j(paginate @post_comments) %>")
