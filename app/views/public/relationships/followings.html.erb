<div class='container'>
	<div class='row justify-content-center pt-3'>
		<h2 class="col-md-auto section-ttl text-center">フォローリスト</h2>
    <% if @members.exists? %>
      <%= render 'public/members/index', members: @members %>
    <% else %>
      <div class='p-0 m-0'>
        <p>メンバーはいません</p>
      </div>
    <% end %>
  </div>
</div>