<div class='container'>
  <h2 class="pt-3" style="text-align: center;">投稿一覧</h2>
	<div class='row justify-content-center pt-3'>
    <% if member_signed_in? %>
      <div class="col-12"><%= render 'public/searches/form' %></div>
    <% end %>
    <%= render "public/books/index", books: @books %>
    <div class="mt-2">
      <%= paginate @books %>
    </div>
  </div>
</div>