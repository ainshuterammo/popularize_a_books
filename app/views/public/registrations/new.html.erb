<div class="new_sign container">
  <div class='row justify-content-center pt-5'>
    <h2 class="col-md-auto section-ttl text-center">新規会員登録</h2>
  </div>
  <div class='row justify-content-center'>
    <div class="col-md-6 px-5 px-sm-0 mx-auto mt-4">
      <%= form_with model: @member, url: registration_path(resource_name), local: true do |f| %>
        <div class="text-center">
          <%= render 'layouts/error_messages', resource: @member %>
        </div>
        <div class="field row mb-3 justify-content-center">
          <%= f.text_field :name, autofocus: true, :placeholder =>"メンバーネーム", class: 'col-6' %></td>
        </div>
        <div class="field row mb-3 justify-content-center">
          <%= f.email_field :email, autocomplete: "email", :placeholder =>"メールアドレス", class: 'col-6' %>
        </div>
        <div class="field row mb-3 justify-content-center">
          <%= f.password_field :password, autocomplete: "new-password", :placeholder =>"パスワード(#{@minimum_password_length}文字以上)", class: 'col-6' %>
        </div>
        <div class="field row mb-3 justify-content-center">
          <%= f.password_field :password_confirmation, autocomplete: "new-password", :placeholder =>"パスワード(確認用)", class: 'col-6' %>
        </div>
        <div class='row justify-content-center'>
          <%= f.submit "新規登録", class: "btn btn-success col-md-6 px-5 px-sm-0 mx-auto my-3" %>
        </div>
      <% end %>
    </div>
  </div>
  <div class='row justify-content-center pb-3'>
    <div class="col-md-auto px-5 px-sm-0 mx-auto mt-4">
      <h2>既に登録済みの方</h2>
        <p><%= link_to "こちら", new_member_session_path, class: "underline" %>からログインしてください。</p>
    </div>
  </div>
</div>
